import { } from "../types/currentReportsTypes";

export const currentReportsReducer = (state = {}, action) => {
  const { type, payload } = action;
  switch (type) {
  
    default:
      return state;
  }
};

export default currentReportsReducer;
